apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-search-website.fullname" . }}
  labels:
    {{- include "code-search-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-search-website.selectorLabels" . | nindent 4 }}
