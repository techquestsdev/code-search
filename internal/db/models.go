// Package db provides database abstractions and utilities.
// The main types (Connection, Repository, etc.) are defined in internal/repos/service.go
// which uses the db.Pool interface for database-agnostic operations.
package db
